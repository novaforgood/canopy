table:
  name: users
  schema: public
select_permissions:
- permission:
    columns:
    - email
    - first_name
    - id
    - last_name
    - created_at
    filter:
      id:
        _eq: X-Hasura-User-Id
  role: user
