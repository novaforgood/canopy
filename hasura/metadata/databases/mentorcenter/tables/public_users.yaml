table:
  schema: public
  name: users
select_permissions:
  - role: user
    permission:
      columns:
        - email
        - first_name
        - id
        - last_name
        - created_at
      filter:
        id:
          _eq: X-Hasura-User-Id
