table:
  name: public_space
  schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - name
        - slug
        - id
      filter: {}
  - role: user
    permission:
      columns:
        - name
        - slug
        - id
      filter: {}
